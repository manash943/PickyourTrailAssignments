
Steps to run the programs:

In the console give the inputs accordingly as mentioned below:

MudWalls:
The first line contains an integer, n, the number of elements in
stickPositions.
Each line i of the n subsequent lines (where 0 ≤ i < n) contains an
integer
The next line contains the integer, n, the number of elements 
Each line i of the n subsequent lines (where 0 ≤ i < n) contains an
integer


PallindromeCounter:
The first line contains a string, s.
